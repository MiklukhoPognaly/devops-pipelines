#!groovy

pipeline {
    agent any

    stage('Copy Archive') {
         steps {
             script {
                step ([$class: 'CopyArtifact',
                    projectName: 'frontend-build',
                    filter: "*.zip"]);
            }
        }
    }
}