#!groovy

pipeline {
    agent any
    stages {
      stage('Copy Archive') {
           steps {
               step ([$class: 'CopyArtifact',
                      projectName: 'frontend-build',
                      filter: "*.zip"])
                }
            }
        }
}